# Local runtime config with real secrets
config/openclaw.json

# Instance data directories (contain secrets and runtime state)
data/

# Common local environment files
.env
.env.*
!.env.example

# OS/editor noise
.DS_Store
Thumbs.db
*.tmp
*.log
